< !DOCTYPE html><html lang="he" dir="rtl"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0"><title>מבחן תרגול 3 - מודולים בפייתון</title><link href="https://fonts.googleapis.com/css2?family=Heebo:wght@300;400;600;700&family=Assistant:wght@300;400;600;700&display=swap" rel="stylesheet"><style>* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --primary: #059669;
    --secondary: #10b981;
    --accent: #8b5cf6;
    --success: #14b8a6;
    --warning: #f59e0b;
    --bg: #ecfdf5;
    --card: #ffffff;
    --text: #1e293b;
    --text-light: #64748b;
    --border: #a7f3d0;
}

body {
    font-family: 'Assistant', 'Heebo', sans-serif;
    background: linear-gradient(135deg, #059669 0%, #10b981 100%);
    min-height: 100vh;
    padding: 2rem;
    color: var(--text);
    line-height: 1.6;
}

.container {
    max-width: 900px;
    margin: 0 auto;
    background: var(--card);
    border-radius: 24px;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    overflow: hidden;
    animation: slideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.header {
    background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
    color: white;
    padding: 2.5rem;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.header::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
    animation: pulse 4s ease-in-out infinite;
}

@keyframes pulse {

    0%,
    100% {
        transform: scale(1);
        opacity: 0.3;
    }

    50% {
        transform: scale(1.1);
        opacity: 0.5;
    }
}

.header h1 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    position: relative;
    z-index: 1;
}

.header p {
    font-size: 1.1rem;
    opacity: 0.95;
    position: relative;
    z-index: 1;
}

.exam-badge {
    background: rgba(139, 92, 246, 0.95);
    color: white;
    padding: 0.5rem 1.5rem;
    border-radius: 20px;
    font-weight: 600;
    display: inline-block;
    margin-top: 1rem;
    position: relative;
    z-index: 1;
}

.exam-info {
    background: linear-gradient(to bottom, #ecfdf5, white);
    padding: 1.5rem 2.5rem;
    border-bottom: 2px solid var(--border);
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
}

.info-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
}

.info-item strong {
    color: var(--primary);
    font-weight: 600;
}

.progress-bar {
    background: var(--bg);
    height: 8px;
    position: relative;
    overflow: hidden;
}

.progress-fill {
    background: linear-gradient(90deg, var(--accent), var(--success));
    height: 100%;
    width: 0%;
    transition: width 0.5s cubic-bezier(0.16, 1, 0.3, 1);
}

.content {
    padding: 2.5rem;
}

.question-card {
    background: white;
    border: 2px solid var(--border);
    border-radius: 16px;
    padding: 2rem;
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
    animation: fadeIn 0.5s ease forwards;
    opacity: 0;
}

@keyframes fadeIn {
    to {
        opacity: 1;
    }
}

.question-card:hover {
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
    transform: translateY(-2px);
}

.question-header {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.question-number {
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    color: white;
    width: 45px;
    height: 45px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.1rem;
    flex-shrink: 0;
    box-shadow: 0 4px 12px rgba(5, 150, 105, 0.3);
}

.question-text {
    flex: 1;
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--text);
    line-height: 1.6;
}

.code-block {
    background: #1e1e1e;
    color: #d4d4d4;
    padding: 1.5rem;
    border-radius: 12px;
    margin: 1rem 0;
    font-family: 'Consolas', 'Monaco', monospace;
    font-size: 0.9rem;
    line-height: 1.6;
    overflow-x: auto;
    box-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.3);
    white-space: pre-wrap;
    direction: ltr;
    text-align: left;
}

.options {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.option {
    background: var(--bg);
    border: 2px solid var(--border);
    border-radius: 12px;
    padding: 1rem 1.25rem;
    cursor: pointer;
    transition: all 0.2s ease;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    position: relative;
}

.option:hover {
    background: #d1fae5;
    border-color: var(--secondary);
    transform: translateX(-3px);
}

.option input[type="radio"] {
    width: 20px;
    height: 20px;
    margin-top: 2px;
    cursor: pointer;
    flex-shrink: 0;
    accent-color: var(--accent);
}

.option label {
    cursor: pointer;
    flex: 1;
    font-size: 0.95rem;
    line-height: 1.5;
}

.option.selected {
    background: linear-gradient(135deg, #e9d5ff, #ddd6fe);
    border-color: var(--accent);
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.2);
}

.option.correct {
    background: linear-gradient(135deg, #d1fae5, #a7f3d0);
    border-color: var(--success);
}

.option.incorrect {
    background: linear-gradient(135deg, #fee2e2, #fecaca);
    border-color: #ef4444;
}

.submit-section {
    text-align: center;
    padding: 2rem;
    border-top: 2px solid var(--border);
    background: linear-gradient(to top, #ecfdf5, white);
}

.btn {
    background: linear-gradient(135deg, var(--accent), #7c3aed);
    color: white;
    border: none;
    padding: 1rem 3rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
    font-family: inherit;
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(139, 92, 246, 0.4);
}

.btn:active {
    transform: translateY(0);
}

.btn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

.results {
    background: white;
    border-radius: 16px;
    padding: 2.5rem;
    margin-top: 2rem;
    text-align: center;
    border: 2px solid var(--border);
    animation: scaleIn 0.5s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes scaleIn {
    from {
        opacity: 0;
        transform: scale(0.9);
    }

    to {
        opacity: 1;
        transform: scale(1);
    }
}

.score-circle {
    width: 180px;
    height: 180px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    color: white;
    box-shadow: 0 10px 30px rgba(5, 150, 105, 0.3);
}

.score-number {
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1;
}

.score-label {
    font-size: 1rem;
    opacity: 0.9;
    margin-top: 0.5rem;
}

.results h2 {
    font-size: 2rem;
    color: var(--primary);
    margin-bottom: 1rem;
}

.results-details {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
    margin-top: 2rem;
}

.result-stat {
    padding: 1rem;
    background: var(--bg);
    border-radius: 12px;
    border: 1px solid var(--border);
}

.result-stat-value {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--primary);
}

.result-stat-label {
    font-size: 0.9rem;
    color: var(--text-light);
    margin-top: 0.25rem;
}

.share-section {
    margin-top: 2rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
}

.share-btn {
    background: linear-gradient(135deg, #10b981, #059669);
    color: white;
    border: none;
    padding: 0.75rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.3s ease;
    margin: 0.5rem;
    font-family: inherit;
}

.share-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(16, 185, 129, 0.3);
}

@media (max-width: 768px) {
    body {
        padding: 1rem;
    }

    .container {
        border-radius: 16px;
    }

    .header h1 {
        font-size: 1.5rem;
    }

    .content {
        padding: 1.5rem;
    }

    .question-card {
        padding: 1.5rem;
    }

    .results-details {
        grid-template-columns: 1fr;
    }
}

.hidden {
    display: none;
}

</style></head><body><div class="container"><div class="header"><h1>מבחן תרגול 3 - מודולים בפייתון</h1><p>המכללה האקדמית תל אביב יפו - קורס 271033</p><div class="exam-badge">מבחן תרגול מתקדם</div></div><div class="exam-info"><div class="info-item"><strong>סוג:</strong><span>תרגול מס' 3</span>
 </div><div class="info-item"><strong>משך מומלץ:</strong><span>שעתיים</span></div><div class="info-item"><strong>שאלות:</strong><span>20 שאלות × 5 נק'</span>
 </div><div class="info-item"><strong>ציון מקסימלי:</strong><span>100 נקודות</span></div></div><div class="progress-bar"><div class="progress-fill" id="progressBar"></div></div><div class="content"><div id="questionsContainer"></div><div class="submit-section"><button class="btn" id="submitBtn">בדוק תשובות</button></div><div id="resultsSection" class="hidden"></div></div></div><script>const questions=[ {
    number: 1,
        text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
        code: `class MyClass: x=10 def __init__(self): self.x=20 obj=MyClass() print(obj.x, MyClass.x)`,
        options: [ 'יודפס 20 10.',
        'יודפס 10 20.',
        'יודפס 20 20.',
        'תתקבל שגיאת זמן ריצה בגלל הגדרה כפולה של x.'
        ],
        correct: 0
}

,
{
number: 2,
text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
code: `def decorator(func): def wrapper(*args, **kwargs): print("Before") result=func(*args, **kwargs) print("After") return result return wrapper @decorator def say_hello(name): print(f"Hello {name}") say_hello("Alice")`,
options: [ 'יודפס Before, Hello Alice, After.',
'יודפס Hello Alice בלבד.',
'תתקבל שגיאת זמן ריצה כי decorator לא מוגדר כראוי.',
'יודפס Before ו-After בלבד.'
],
correct: 0
}

,
{
number: 3,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `numbers=[1, 2, 3, 4, 5, 6] result=[x for x in numbers if x % 2==0] result2=list(filter(lambda x: x % 2==0, numbers)) print(result==result2)`,
    options: [ 'יודפס True.',
    'יודפס False.',
    'תתקבל שגיאת זמן ריצה כי לא ניתן להשוות רשימות.',
    'יודפס None.'
    ],
    correct: 0
}

,
{
number: 4,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `class Parent: def __init__(self): self.value="Parent"

    def show(self): print(self.value) class Child(Parent): def __init__(self): super().__init__() self.value="Child"

    c=Child() c.show()`,
    options: [ 'יודפס Child.',
    'יודפס Parent.',
    'תתקבל שגיאת זמן ריצה.',
    'לא יודפס כלום.'
    ],
    correct: 0
}

,
{
number: 5,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `def gen_numbers(): for i in range(3): yield i * 2 g=gen_numbers() print(next(g)) print(next(g))`,
    options: [ 'יודפס 0 ואז 2.',
    'יודפס 0 1 2 3 4 5.',
    'תתקבל שגיאת זמן ריצה כי yield לא תקין.',
    'יודפס <generator object>.'
    ],
    correct: 0
}

,
{

number: 6,
text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
code: `d= {
    'a': 1, 'b': 2, 'c': 3
}

result= {
    k: v*2 for k, v in d.items() if v > 1
}

print(result)`,
options: [ 'יודפס {\'b\': 4, \'c\': 6}.',
'יודפס {\'a\': 2, \'b\': 4, \'c\': 6}.',
'תתקבל שגיאת זמן ריצה.',
'יודפס {}.'
],
correct: 0
}

,
{
number: 7,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `try: x=int("abc") except ValueError as e: print("Error:", type(e).__name__) except Exception: print("General Error")`,
    options: [ 'יודפס Error: ValueError.',
    'יודפס General Error.',
    'יודפס Error: Exception.',
    'התוכנית תקרוס.'
    ],
    correct: 0
}

,
{
number: 8,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `from functools import reduce numbers=[1, 2, 3, 4, 5] result=reduce(lambda x, y: x + y, numbers) print(result)`,
    options: [ 'יודפס 15.',
    'יודפס [1, 2, 3, 4, 5].',
    'תתקבל שגיאת זמן ריצה כי reduce לא קיים.',
    'יודפס 120.'
    ],
    correct: 0
}

,
{
number: 9,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `class Meta(type): def __new__(cls, name, bases, dct): dct['custom']=True return super().__new__(cls, name, bases, dct) class MyClass(metaclass=Meta): pass print(hasattr(MyClass, 'custom'))`,
    options: [ 'יודפס True.',
    'יודפס False.',
    'תתקבל שגיאת זמן ריצה כי metaclass לא מוגדר כראוי.',
    'יודפס None.'
    ],
    correct: 0
}

,
{
number: 10,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `x=[1, 2, 3] y=x.copy() y.append(4) print(len(x), len(y))`,
    options: [ 'יודפס 3 4.',
    'יודפס 4 4.',
    'יודפס 3 3.',
    'תתקבל שגיאת זמן ריצה.'
    ],
    correct: 0
}

,
{
number: 11,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `def outer(x): def inner(y): return x + y return inner add_five=outer(5) print(add_five(10))`,
    options: [ 'יודפס 15.',
    'יודפס 5.',
    'תתקבל שגיאת זמן ריצה כי x לא מוגדר ב-inner.',
    'יודפס 10.'
    ],
    correct: 0
}

,
{
number: 12,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `class A: def method(self): print("A") class B(A): def method(self): print("B") super().method() b=B() b.method()`,
    options: [ 'יודפס B ואז A.',
    'יודפס A ואז B.',
    'יודפס B בלבד.',
    'תתקבל שגיאת זמן ריצה.'
    ],
    correct: 0
}

,
{
number: 13,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `import sys def recursive(n): if n==0: return 0 return n + recursive(n - 1) print(recursive(5))`,
    options: [ 'יודפס 15.',
    'יודפס 5.',
    'התוכנית תיכנס ללולאה אינסופית.',
    'תתקבל שגיאת זמן ריצה בגלל RecursionError.'
    ],
    correct: 0
}

,
{
number: 14,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `class Singleton: _instance=None def __new__(cls): if cls._instance is None: cls._instance=super().__new__(cls) return cls._instance s1=Singleton() s2=Singleton() print(s1 is s2)`,
    options: [ 'יודפס True.',
    'יודפס False.',
    'תתקבל שגיאת זמן ריצה.',
    'יודפס None.'
    ],
    correct: 0
}

,
{
number: 15,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `numbers=[1, 2, 3, 4, 5] squared=map(lambda x: x**2, numbers) print(type(squared).__name__)`,
    options: [ 'יודפס map.',
    'יודפס list.',
    'יודפס generator.',
    'תתקבל שגיאת זמן ריצה.'
    ],
    correct: 0
}

,
{
number: 16,
    text: "בחרו את הטענה הלא נכונה העוסקת ב-decorators בפייתון:",
    options: [ 'decorator הוא פונקציה שמקבלת פונקציה ומחזירה פונקציה.',
    'ניתן להשתמש במספר decorators על אותה פונקציה.',
    'decorators חייבים להשתמש ב-@wraps מהמודול functools.',
    'decorators יכולים לקבל פרמטרים.'
    ],
    correct: 2
}

,
{
number: 17,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `class MyContext: def __enter__(self): print("Enter") return self def __exit__(self, exc_type, exc_val, exc_tb): print("Exit") return False with MyContext() as ctx: print("Inside")`,
    options: [ 'יודפס Enter, Inside, Exit.',
    'יודפס Enter, Exit.',
    'יודפס Inside בלבד.',
    'תתקבל שגיאת זמן ריצה.'
    ],
    correct: 0
}

,
{
number: 18,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `a=[1, [2, 3], 4] b=a.copy() b[1].append(5) print(a)`,
    options: [ 'יודפס [1, [2, 3, 5], 4].',
    'יודפס [1, [2, 3], 4].',
    'יודפס [1, [2, 3], 4, 5].',
    'תתקבל שגיאת זמן ריצה.'
    ],
    correct: 0
}

,
{
number: 19,
    text: "בחרו את הטענה המדויקת ביותר העוסקת בקטע הקוד הבא:",
    code: `from collections import defaultdict d=defaultdict(int) d['a'] +=1 d['b'] +=2 print(d['c'])`,
    options: [ 'יודפס 0.',
    'תתקבל שגיאת KeyError.',
    'יודפס None.',
    'יודפס 3.'
    ],
    correct: 0
}

,
{
number: 20,
    text: "בחרו את הטענה הלא נכונה העוסקת בירושה מרובה (multiple inheritance) בפייתון:",
    options: [ 'מחלקה יכולה לרשת ממספר מחלקות אב.',
    'סדר הירושה נקבע לפי Method Resolution Order (MRO).',
    'ניתן לגשת ל-MRO באמצעות __mro__.',
    'בירושה מרובה, כל המתודות מכל מחלקות האב חייבות להיקרא.'
    ],
    correct: 3
}

];

let userAnswers=new Array(questions.length).fill(null);
let submitted=false;

function renderQuestions() {
    const container=document.getElementById('questionsContainer');

    questions.forEach((q, index)=> {
            const card=document.createElement('div');
            card.className='question-card';

            card.style.animationDelay=`$ {
                index * 0.05
            }

            s`;

            let html=` <div class="question-header" > <div class="question-number" >$ {
                q.number
            }

            </div> <div class="question-text" >$ {
                q.text
            }

            </div> </div> `;

            if (q.code) {
                html +=`<div class="code-block" >$ {
                    escapeHtml(q.code)
                }

                </div>`;
            }

            html +='<div class="options">';

            q.options.forEach((option, optIndex)=> {
                    html +=` <div class="option" data-question="${index}" data-option="${optIndex}" > <input type="radio" name="q${index}" id="q${index}_${optIndex}" value="${optIndex}" > <label for="q${index}_${optIndex}" >$ {
                        String.fromCharCode(97 + optIndex)
                    }

                    . $ {
                        option
                    }

                    </label> </div> `;
                });
            html +='</div>';

            card.innerHTML=html;
            container.appendChild(card);
        });

    document.querySelectorAll('.option').forEach(opt=> {
            opt.addEventListener('click', function() {
                    if (submitted) return;

                    const qIndex=parseInt(this.dataset.question);
                    const optIndex=parseInt(this.dataset.option);
                    const radio=this.querySelector('input[type="radio"]');
                    radio.checked=true;

                    this.parentElement.querySelectorAll('.option').forEach(o=> {
                            o.classList.remove('selected');
                        });
                    this.classList.add('selected');

                    userAnswers[qIndex]=optIndex;
                    updateProgress();
                });
        });
}

function updateProgress() {
    const answered=userAnswers.filter(a=> a !==null).length;
    const percentage=(answered / questions.length) * 100;
    document.getElementById('progressBar').style.width=percentage+'%';
}

function escapeHtml(text) {
    const div=document.createElement('div');
    div.textContent=text;
    return div.innerHTML;
}

function submitExam() {
    if (submitted) return;

    const unanswered=userAnswers.filter(a=> a===null).length;

    if (unanswered > 0) {
        if ( !confirm(`יש לך $ {
                    unanswered
                }

                שאלות ללא תשובה. האם אתה בטוח שאתה רוצה לשלוח?`)) {
            return;
        }
    }

    submitted=true;
    document.getElementById('submitBtn').disabled=true;
    document.getElementById('submitBtn').textContent='המבחן נבדק';

    let correct=0;

    document.querySelectorAll('.question-card').forEach((card, qIndex)=> {
            const options=card.querySelectorAll('.option');
            const correctAnswer=questions[qIndex].correct;
            const userAnswer=userAnswers[qIndex];

            options.forEach((opt, optIndex)=> {
                    if (optIndex===correctAnswer) {
                        opt.classList.add('correct');
                    }

                    if (userAnswer !==null && optIndex===userAnswer && userAnswer !==correctAnswer) {
                        opt.classList.add('incorrect');
                    }
                });

            if (userAnswer===correctAnswer) {
                correct++;
            }
        });

    showResults(correct);
}

function showResults(correct) {
    const score=correct * 5;
    const percentage=(correct / questions.length) * 100;
    const wrong=questions.length - correct - userAnswers.filter(a=> a===null).length;
    const unanswered=userAnswers.filter(a=> a===null).length;

    let grade='';
    if (percentage >=90) grade='מצוין! 🌟';
    else if (percentage >=80) grade='טוב מאוד! 👏';
    else if (percentage >=70) grade='יפה! 👍';
    else if (percentage >=60) grade='עבר ✓';
    else grade='צריך לשפר 📚';

    const resultsHtml=` <div class="results"><div class="score-circle"><div class="score-number">$ {
        score
    }

    </div><div class="score-label">מתוך 100</div></div><h2>$ {
        grade
    }

    </h2><div class="results-details"><div class="result-stat"><div class="result-stat-value" style="color: #10b981;">$ {
        correct
    }

    </div><div class="result-stat-label">תשובות נכונות</div></div><div class="result-stat"><div class="result-stat-value" style="color: #ef4444;">$ {
        wrong
    }

    </div><div class="result-stat-label">תשובות שגויות</div></div><div class="result-stat"><div class="result-stat-value" style="color: #f59e0b;">$ {
        unanswered
    }

    </div><div class="result-stat-label">לא נענו</div></div></div><div class="share-section"><p style="color: var(--text-light); margin-bottom: 1rem;">שתף את התוצאה שלך עם חברים !</p><button class="share-btn" onclick="shareResults(${score})">📱 שתף בווטסאפ</button><button class="share-btn" onclick="copyResults(${score})">📋 העתק קישור</button></div></div>`;

    document.getElementById('resultsSection').innerHTML=resultsHtml;
    document.getElementById('resultsSection').classList.remove('hidden');

    document.getElementById('resultsSection').scrollIntoView({
        behavior: 'smooth', block: 'nearest'
    });
}

function shareResults(score) {
    const text=`סיימתי מבחן תרגול 3 במודולים בפייתון וקיבלתי $ {
        score
    }

    /100 ! 🎓`;
    const url=`https: //wa.me/?text=${encodeURIComponent(text)}`;
    window.open(url, '_blank');
}

function copyResults(score) {
    const text=`סיימתי מבחן תרגול 3 במודולים בפייתון וקיבלתי $ {
        score
    }

    /100 ! 🎓`;
    navigator.clipboard.writeText(text).then(()=> {
            alert('הועתק ללוח! ✓');
        });
}

renderQuestions();
document.getElementById('submitBtn').addEventListener('click', submitExam);
</script></body></html>